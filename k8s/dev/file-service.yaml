apiVersion: apps/v1
kind: Deployment
metadata:
  name: file-service
  namespace: circleguard-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: file-service
  template:
    metadata:
      labels:
        app: file-service
    spec:
      containers:
        - name: file-service
          image: circleguard/file-service:dev
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: dev
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: file-service
  namespace: circleguard-dev
spec:
  selector:
    app: file-service
  ports:
    - port: 8080
      targetPort: 8080
